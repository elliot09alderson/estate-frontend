# API Configuration
# For development (local backend)
VITE_API_BASE_URL=http://localhost:3001/api

# For production (deployed backend)
# VITE_API_BASE_URL=https://estate-backend-th8i.onrender.com/api

# Note: Create .env.production file with production URL for deployed version
# The app will automatically fallback to production URL if no env var is set